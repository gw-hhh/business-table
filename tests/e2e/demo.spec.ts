import{test,expect}from'@playwright/test';test('demo renders and core interactions work',async({page})=>{const errors:string[]=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});await page.goto('/');await expect(page.locator('[data-business-table]')).toBeVisible();await expect(page.getByText('Q20260901-0001').first()).toBeVisible();await page.getByTestId('column-settings').click();await expect(page.getByTestId('column-panel')).toBeVisible();await page.getByTitle('左冻结 报价编号').click();await page.getByRole('button',{name:'下一页'}).isVisible();expect(errors).toEqual([])})