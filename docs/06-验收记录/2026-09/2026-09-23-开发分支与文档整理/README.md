# 开发分支与文档整理验收

日期：2026-09-23。目标分支：development。

## 范围

按用户要求合并现有功能分支，保留 main 和 development，清理已合并功能分支；整理七类中文文档、版本模板、月份索引、历史材料与现行规则。参考 NewBusiness-Vue 的组织方式，BusinessTable 的平台设计和旧版界面参照保持各自职责。

## 修改文件与流程调整

- `docs/`：原说明、计划、验收与归档迁到中文目录；新增总导航、分类与月份索引、版本说明和升级模板。
- 根 `README.md`、`AGENTS.md`、`CHANGELOG.md`：同步入口、阅读顺序和 development 优先流程。
- `scripts/docs-audit.mjs`、`tests/docs-audit.spec.ts`、`package.json`：文档路径、锚点、命名、版本序号和可达性进入发布检查，12 项独立临时仓库用例验证检查工具本身。
- `.github/workflows/`：保留 development/main 自动门禁，将原功能分支的自动交付打包移到 development，同时保留手动触发；删除旧功能分支专用 refinement-review 验证。
- 根旧交付 JSON 与启动说明移入历史交付目录，机器内容保持不变；`reference/`、业务实现和依赖版本未改。

## 分支证据

- 合并前 origin/development：`9935c74`。
- 功能分支本地最终提交：`53ef177`；远端原功能分支：`761bd3a`。
- 已验证 origin/development 是功能分支祖先，并将本地 development 快进至 `53ef177`，完整保留提交历史。
- main 原提交：`d06979f428cf52bd401a1e41589c190272474a56`，本任务保留。
- 本记录保存本地合并及可重复验证证据；远端推送、功能分支清理的最终提交与状态以本批交付回复和 Git 记录为准，不将本地检查预写为远端 CI 已通过。

## 检查结果

| 检查 | 实际结果 |
| --- | --- |
| 文档审计 | 50 篇 docs Markdown 的目录、链接、锚点、月份及所属索引检查通过；根 README、AGENTS、CHANGELOG 一并检查 |
| 审计工具回归 | 12/12 通过；先复现缺月份索引、跨目录索引遗漏和本地绝对路径漏报，再修复并复测 |
| TypeScript | 最终类型检查通过 |
| Vitest | 最终 55 个文件、433 项通过 |
| 可移植性与 SFC | 通过；41 个 Vue SFC |
| 构建 | 组件库、声明和 Demo 构建通过 |
| 发布包 | 188 个文件，独立 TypeScript 消费方检查通过 |
| 浏览器 | 113/113 通过，开发服务与构建预览四类运行错误为 0 |
| 源码边界 | src、demo、reference 和锁文件无变更；原交付 JSON 与启动说明内容保留 |
| 独立审查 | 已处理审计漏报及旧规则矛盾，未遗留重要问题 |

完整 `npm run verify:release` 退出 0，日志为 `.vite/development-docs-release.log`（该轮 432 项单元测试）。随后新增 file URI 负例并修复审计，定向 12/12 通过，重新运行最终类型检查与全部单元测试得到 433 项通过，日志为 `.vite/development-docs-final-unit.log`。补测阶段没有改变业务源码和浏览器用例，不重复声称执行过另一轮完整浏览器检查。

## 保留边界

- 离线文档审计检查仓库本地链接和索引，不验证外部网站持续可访问。
- 本轮无业务 UI 改动，不新增视觉一致性或真实后台已通过的结论。
- 大体积门禁日志保留在本地 `.vite/` 忽略目录；远端执行记录在对应 GitHub Actions 中，不能将本地检查直接称为远端通过。

## 关联材料

- [版本说明](../../../04-版本记录/2026-09/2026-09-23-04-开发分支与文档体系整理.md)
- [实施计划](../../../05-实施计划/2026-09/2026-09-23-开发分支与文档整理计划.md)
- [文档导航](../../../README.md)
- [返回本月验收](../README.md)
