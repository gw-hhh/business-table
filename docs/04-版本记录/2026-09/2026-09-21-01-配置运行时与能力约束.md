# 配置运行时与能力约束

## 基本信息

- 日期：2026-09-21。2026-09-23 按已有验收补录，未重新执行历史测试。
- 类型：功能迁移与修复。
- 关联提交：`550efb2`
- 产品版本：原记录不构成新的正式版本发布。

## 本次变更

新增 ConfiguredBusinessTable、版本化偏好、Feature Gate、Registry 和列能力 Guard。

## 影响与验证

104 项 Vitest、14 项 Playwright；原记录明确本地验证与远端 CI 的区别。 详细修改文件、兼容影响、失败路径和证据均保留在[原验收记录](../../06-验收记录/2026-09/2026-09-21-配置运行时/README.md)。当时的分支和推送状态只描述历史，当前流程见[开发规则](../../02-架构与规范/02-开发规则.md)。

## 升级、回退与未完成项

本记录仅归档已存在的变更，不执行安装或数据迁移。旧批次的接口变化与限制以原验收为准；当前安装入口见[本地运行与接入](../../01-项目入门/02-本地运行与接入.md)。需要回退时选择目标已验证提交并先保存业务数据，不因回退源码覆盖本地存储。

最新能力与待办见[当前状态](../../01-项目入门/03-当前状态.md)和[后续路线图](../../01-项目入门/04-后续路线图.md)，后续补齐能力不回写为本批已完成。

[返回本月版本](README.md)

## 原根版本日志摘要

- 新增 ConfiguredBusinessTable、版本化差量偏好、局部配置诊断和列能力 Guard。
- 新增代码优先的 Feature Gate、延迟 UI 加载、列设置 default/custom/headless 和稳定 ID Registry。
- 最简 BusinessTable 默认仅表格与分页；旧调用需显式开启 search/toolbar/columnSettings，旧 title/views/actions 声明继续有效。
- View 列布局不再改写个人偏好；存储异常不再阻止数据加载；禁用操作保留界面且不执行。
- 保留原 schema1 Persistence、ESM/UMD 分发和现有 Provider 并发回归；未升级核心依赖。
