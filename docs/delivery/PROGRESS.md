# 2026-09-22 交付记录

## 源码基线

当前从用户收到的 `business-table-refinement-source.zip` 恢复未推送的精修代码；该源码包记录本地快照48946a8，来自feature/config-runtime@550efb2。远端目标继续使用feature/visual-parity-refinement，不覆盖config-runtime、development或main。

远端e9902d7的GitHub Actions source.tar已恢复，git write-tree精确等于df3dc244fe6ca63efba97a4667e11295c5985187。参考目录保留145个原始文件，不通过重建改写。

上一轮提到的全量补齐源码没有随日志附上，本轮工作区也没有这部分未归档源文件。`full-parity-evidence`只有3份测试/构建日志。因此这些功能不能标为已交付；重新实现与验证时会以实际提交为准。

## 已恢复（原精修代码，不冒称本轮新增）

字体搜索、静态字体字典、颜色输入与常用色板、非法颜色草稿保留及定位、分段对齐键盘导航、快捷列触屏移动、隐藏列划线、当前列粘性标题、弃改确认，以及配套14项单测/7个浏览器场景。

恢复后的Vitest实测151/151通过。全量恢复门禁结果单独留证，不以此称全部迁移完成。

## 尚未恢复、不得作为完成项

五个设置页全量功能、共享单元格管线、映射/模板/试算、数据工具、完整导出、架构审查中的可靠性问题，以本轮真实源码与覆盖记录重新验收。Final Spec与Execution Brief未包含在现有源码包中；已有Master Handoff与用户明确旧版对齐要求继续执行，不虚构缺失规范。

## 推送边界

用户已明确要求推送所有改动。保存阶段代码到功能分支不等于发布到生产。最终发布必须包含源码、可用类型声明、组件包、Demo构建、运行测试与逐入口验收清单；未达到全量验收前不移动main，不标记正式Release。

## 可靠性修复（本轮实测）

审查6个用例在恢复版上5失败/1通过；修复后6/6，完整Vitest157/157。配置保存成功后才提交内存值与释放View覆盖，失败返回设置会话；写入串行防乱序；内置搜索与公开setQuery同步；分页初始化、恢复与更新共用normalizer；可选偏好提供3秒默认可配置超时与卸载取消。包审计先复现缺少类型入口，再修正构建顺序与声明图，独立TypeScript消费真实npm包通过。未把这些修复标为全量UI完成。
