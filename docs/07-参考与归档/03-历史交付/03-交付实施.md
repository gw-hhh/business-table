# 完整迁移与交付执行记录

> 历史记录：保留本批发生时的结果、分支和限制，不作为当前开发规则。2026-09-23 归档时仅调整文档路径；当前状态与分支流程请从[文档导航](../../README.md)阅读。

基线：可恢复精修源码包（48946a8），从feature/config-runtime@550efb2派生。执行规范：docs/02-架构与规范/01-平台总体设计.md及用户旧版全功能/UI对齐要求。旧reference只读；不升级依赖，不用旧JS运行时，不用iframe；正常生产包与只读预览共用显示规则；不自动移动main。

本轮直接执行，不再请求分批确认。任何没有源码、没有实际证据的进度不算完成。

## 顺序

- [x] 恢复已交付源码，核对e9902d7参考树和151项单测，记录上轮全量代码未归档事实。
- [ ] 将上一轮6项审查探针放入本次套件，先复现；修正保存失败、搜索同步、所有分页入口、可选偏好超时和包类型产物。
- [ ] 提取共享配置/查询/提交边界，列与高级展示使用自己的可序列化协议，能力约束贯穿各个入口。
- [ ] 补全五类设置草稿、列规则与主表/预览的真实联动，控件从旧版真实源码及操作测量取得。
- [ ] 补全数据工具、导出与备份兼容路径，禁止只显示按钮/禁用页签冒充实现。
- [ ] 枚举旧版静态入口与动态菜单，逐条映射新实现及行为测试，并记录无法确认项。
- [ ] 全量单测/浏览器/库与Demo/独立消费包检查，自审关键失败路径。
- [ ] 推送所有实际源码与测试，远端CI重新通过，附完整源码、组件包、Demo和证据。

## 重点边界

保存失败/重复点击；偏好挂起/晚返回；本地false远端true；动态能力收窄；多个表实例；空值与有类型的映射；格式不会改写原始数据；预览无副作用；恶意模板/链接/CSV公式；JSON损坏/版本冲突；行宽变小导致动作溢出；窄屏弹层/键盘焦点；CSS与个人配置互不覆盖。
