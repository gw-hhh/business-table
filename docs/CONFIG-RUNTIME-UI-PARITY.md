# config-runtime → legacy v3.1 界面精修矩阵

本文件补充 `LEGACY-MIGRATION.md` 第9节，记录2026-09-22的新一轮修复，不沿用main原始实现的缺口结论。

## 基线和约束

- 按用户本次最新要求，从 `feature/config-runtime@550efb28da9a8108005cac379cc0bb7235aa597e` 新建 `feature/config-runtime-ui-parity`。原分支、main、development均不修改，不自动合并发布。
- main@d06979f的Release Gate已在本次会话重跑通过；新分支继承的config-runtime产品代码也经远端run `35681626214`完整Release Gate验证通过。本地单测重新执行137/137通过。
- 完整阅读最高优先级 `docs/BUSINESS-TABLE-CODEX-MASTER-HANDOFF.md`（100节）、本分支项目上下文、迁移矩阵、根README、reference README。此前main里找不到交接文件的结论不适用于本分支。
- 唯一产品对照为 `reference/legacy-v3.1/quotation-manager-v3.1/index.html` 的真实最终CSS及交互。原包只读，不import旧运行时；保留Feature Gate、Capability Guard、自有配置协议、草稿、Provider生命周期和已有137/32回归。
- 当前Demo默认是客户视图、展开查询和舒适密度。视觉比对先通过公开UI统一双方状态，不把默认视图差异误判为CSS问题，不删除用户已保存的配置。
- PR #4暂停，仅保存main差异的历史RED证据；本轮工作在PR #5。完成与剩余范围以本文件实际证据为准。

## 精修矩阵

| 编号 | 范围 | 基线发现 / 检查目标 | 实现与验证方式 | 状态 |
|---|---|---|---|---|
| R01 | 页头字体、字重、色彩、间距 | 新版页头仍有不同的字重、行高和颜色值；不能用截图中的滤镜/状态替代真实CSS | 同一视口直接对比两页computedStyle和几何；先RED再改作用域样式 | 对照中 |
| R02 | 查询、表头、正文、状态标签、分页 | 既有测试主要校验几何，未比较最终颜色/字体/圆角及不同密度的实际行高 | 新增浏览器样式契约和真实截图；保留大字号及固定底栏既有回归 | 对照中 |
| R03 | 页面更多菜单与密度菜单键盘 | 现有页面菜单仅监听Escape；旧版还支持方向键、Home/End、Tab关闭与回焦 | 先补真实键盘操作测试；不把查询/视图状态搬出原Runtime | 待修复 |
| R04 | 快捷列、完整抽屉与小屏细节 | 保留已实现草稿和能力保护，继续检查焦点、窄屏边界及可操作尺寸 | 320/390/768/1440实际浏览器；不放宽旧断言，不隐藏失败入口 | 对照中 |
| R05 | 设置控件及预览一致性 | 主副文字、排序/冻结状态、修改前后正式表隔离仍须保留 | 沿用现有组件和renderer；新增场景先RED；确认/取消/失败回归 | 对照中 |
| R06 | 完整性边界 | 原矩阵未完成的映射/模板/复杂规则/高级导出等不因精修而成为已完成 | 保留第9节剩余缺口，按本轮实际改动逐项记录 | 仍未完成 |

## 执行

本轮属于已有页面的UI/交互修复，不重建产品架构。先提交本矩阵，再增加新旧页面的同状态浏览器对照、真实键盘回归；核实RED原因后修改现有CSS和菜单代码。完整Release Gate必须在最终修复代码上重新通过，且保存截图及0浏览器错误证据。任何未覆盖、未实现或未验证项必须在交付说明中明确。
