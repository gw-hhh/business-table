# config-runtime → legacy v3.1 界面精修矩阵

本文件补充 `LEGACY-MIGRATION.md` 第9节，记录2026-09-22的新一轮实际修复，不沿用main原始实现的缺口结论。局部验收通过不代表整个旧版已经1:1迁移。

## 基线和约束

- 按用户本次最新要求，从 `feature/config-runtime@550efb28da9a8108005cac379cc0bb7235aa597e` 新建 `feature/config-runtime-ui-parity`。原分支、main、development未由本轮修改，不自动合并发布。
- main@d06979f的Release Gate已在本次会话重跑通过（run35574878411/job106596473058）。新分支继承的config-runtime产品代码也经run35681626214完整Release Gate验证通过，基线为137个单元测试、32个Chromium E2E。
- 已完整阅读最高优先级 `docs/BUSINESS-TABLE-CODEX-MASTER-HANDOFF.md`（100节）、本分支项目上下文、迁移矩阵、根README、reference README。此前main里找不到交接文件的结论不适用于本分支。
- 产品对照为 `reference/legacy-v3.1/quotation-manager-v3.1/index.html` 的真实最终CSS及交互。原包只读，不import旧运行时；保留Feature Gate、Capability Guard、自有配置协议、草稿和Provider生命周期。
- 当前Demo默认是客户视图、展开高级查询和舒适密度。比对通过公开UI统一为全部报价、收起高级条件、标准密度、每页10条；原有业务数据、默认视图、权限限制与个人配置不清除。因此两版记录内容、顺序和未保存提示仍可能不同。
- PR #4暂停，仅保留main差异的历史RED证据。本轮在 [PR #5](https://github.com/gw-hhh/business-table/pull/5)，目标development，保持草稿、未合并。

## 本轮矩阵

| 编号 | 范围 | 实际修改 / 验证 | 结果及边界 |
|---|---|---|---|
| R01 | 页头及基本文字 | 还原标题字重600、22px/手机20px、行高、颜色、面包屑7px间距；补回报价列表h2和分隔线 | 已修复；1440/390实测对应computedStyle与旧版一致，不等于整页逐像素相同 |
| R02 | 控件、正文辅助层级、状态和分页 | 查询输入36px/14px、边框/圆角/33px左内边距；按钮字体颜色、客户副行、草稿和合同标签、分页辅助色；保留大字号相对行高 | 已修复上述子项；9组语义样式在开发/生产双环境比较。表头全部槽位、所有密度/样式组合仍不能由这些子项推断完成 |
| R03 | 图标 | 文件、搜索、刷新、排序、列、方向等使用旧版静态绘制；常规描边1.7，更多/拖动点3；保留左右两个实体冻结图标 | 已修复；10个图标单测。正式代码不依赖旧版JS |
| R04 | 列宽比例 | 编号316→194、金额138→180、状态268→112、日期138→136；项目列保持弹性，最小280；负责人120 | 已修复默认定义；稳定ID/可配置限制不变；用户已保存的列宽仍优先生效 |
| R05 | 页面更多/密度菜单 | ArrowDown/Up打开首尾项，方向键循环、Home/End、Enter选取、Escape回焦、Tab离开；不截获查询输入方向键 | 已修复；5个组件单测及开发/生产真实键盘回归 |
| R06 | 小屏弹层 | 菜单按实际clientWidth/clientHeight扣除滚动条定位，8px边距，超高内部滚动；resize重新定位 | 已修复；320/768视图弹层完整位于视口内，设置抽屉应用按钮可达；保留原有设置草稿回归 |
| R07 | 手机固定列遮挡 | 390px截图发现编号+操作固定层遮住项目内容；现在按实际表格容器宽度≤700临时取消选择/数据/操作固定，变宽后恢复 | 已修复；真实项目文字命中测试、内部横滚到行操作菜单、恢复桌面固定层、保存配置零写入。不是清除用户冻结设置 |
| R08 | 尺寸观察器稳定性 | 容器宽度变化合并到下一帧，Vue列补丁完成后显式重算VXE；关闭VXE自身/父容器自动尺寸反馈；同宽报告不重算，卸载取消排队帧 | 已修复；原生ResizeObserver报错先复现后修复，未忽略浏览器错误。单测覆盖延迟/合并/重复通知/卸载 |
| R09 | 完整设置及高级能力 | 原矩阵未完成的操作按钮/外观/工具栏完整编辑、映射、模板、复杂规则和高级导出等 | 仍未完成，本轮没有伪装为完成；设置预览及已有能力只做回归，不声称重新实现 |

## TDD和真实验证

1. 先提交本矩阵（c18482a），再提交浏览器差异测试（52e3132）。run35682280458真实复现字体/颜色、缺少列表标题、键盘菜单和小屏浮层问题。
2. 图标/菜单专项先出现14个预期失败；列宽单测复现194期望与316实际。实现75ad167后155项中的当时153个单测及run35683520800完整门禁通过。
3. 作者检查真实手机截图，发现首轮绿色未覆盖的固定列遮挡。先提交700692a的浏览器回归，run35683922815报告“应为0个固定遮罩，实际2个”。不能以首轮门禁绿色结束手机验收。
4. b0813d9消除遮挡，但run35684411459仍有4项失败：两项测试选错了VXE外层滚动容器，两项原生ResizeObserver循环错误。根据已锁定VXE源码将测试定位到真正处理scroll事件的`body-inner-wrapper`，保留横滚和菜单断言；另补RED单测证明观察器回调直接改变布局。
5. 最终产品修复提交：`8ebb9c18ac8f98a4867730e5c76d15a4814ae050`。对应 [完整Release Gate](https://github.com/gw-hhh/business-table/actions/runs/35685131907) **SUCCESS**，job106610229478。

| 检查 | 最终产品代码结果 |
|---|---|
| frozen install / Chromium安装 | PASS |
| portability audit / SFC audit / vue-tsc | PASS，15个Vue SFC |
| Vitest / mount | 155/155 PASS，19个测试文件 |
| 声明 / library / Demo build | PASS |
| Chromium开发页和生产预览 | 46/46 PASS，新增样式/键盘/窄屏场景均进入生产预览测试 |
| console.error / pageerror / window.error / unhandledrejection | 对应浏览器回归均为0，检查未删改 |
| 旧版原包完整性 | 145个文件逐一SHA-256一致，0差异 |

原有UMD混合导出、Demo大包和Actions运行时弃用提示仍存在，本轮没有掩盖或声称消除。

## 截图与审查

最终CI产物 `config-runtime-ui-evidence` 保存1440/390新旧页面、320/768视图与设置抽屉、手机项目可读性截图。截图是实际运行结果，不是重新绘制的设计稿。已由作者检查桌面、手机和设置截图；作者自查不能替代独立审查，本轮没有独立审查代理、没有自动合并或发布。

已作出的边界决定：保留config-runtime既有业务数据、默认视图与用户差量，避免为了截图一致而覆盖真实配置；使用700px容器断点实现旧版手机冻结退化，桌面恢复而不持久化；把原生尺寸问题作为阻断项修复，不提高错误容忍度。剩余高级功能继续以Master Handoff和原迁移矩阵为准。
