# 增量实现记录
- 已确认 ZIP 与 v2.0.0 完整源码一致，独立解包，原文件未更改。
- 基线单元测试：56/56 通过。
- 用户已明确要求按讨论方案直接修改；本次直接在隔离副本实现，不再反复询问同一方案。
- 规则第一轮 RED 16 项失败，实现后 16/16；后续加入精确筛选、类型化导出、富文本一致性等测试。
- 新 UI 第一轮 4 项缺失失败；实现后4/4，扩展至16项后16/16通过。
- 决定：依赖 CDN/注册表资源在当前环境无法下载，采用原生 Selection/Range 的轻量结构化编辑器，不把本实现宣称为 Quill。保持离线单文件和受控格式；没有添加第三方字体。
- 原版浏览器基础45项基线通过；现继续跑完整回归。旧测试只更新了新增设置页数量与schema版本的精确断言。
- 条件筛选保留创建时的数字基数；金额相等使用有理数交叉比较，不因0.29浮点乘法而漏记录。
- 新增分组、对比为独立只读面板，而非替换主列表；保留原主表排序、分页及操作。
- 编辑器增加粘贴节点数量/嵌套深度限制，超限时保留原内容并提示，不把异常 HTML 挂到页面。专项 RED 0/1 -> GREEN 1/1。
- 所有 JS/CJS 使用本机 TypeScript printer 统一格式化（仅开发工具，不属于产品依赖），语法检查通过。开始最终全部回归。
- 最终冻结图标回归暴露320px下快捷列按钮被工具栏自动收起。保留旧入口：将快捷列按钮默认设为固定，仍允许用户主动调整；原失败用例完整重跑，而非删改测试。

- 最终完整 test:all 退出0：85项单元、128项浏览器；6份独立XLSX导入及2份渲染通过。真实file/HTTP导航被环境策略阻止，单独记录为未完成。
- 同源单文件重复构建校验一致：629e108707b4bd07cb65043f82c3938e86322f5301e342777ddbef257d67abc2。完整结果见 ../TEST-REPORT.md。
