# BusinessTable 项目工作规则

BusinessTable 是 Vue 3 + TypeScript 通用业务表格平台。报价 Demo 提供数据、字段和业务动作；共享能力进入 Feature、Runtime、Config、Persistence。旧版只用于行为、布局和交互对照。

## 开始前

先读 [README](README.md) 与以下核心文档，再检查实际代码和 Git 状态：

1. [项目背景](docs/01-项目入门/01-项目背景.md)
2. [平台总体设计](docs/02-架构与规范/01-平台总体设计.md)
3. [开发规则](docs/02-架构与规范/02-开发规则.md)
4. [当前状态](docs/01-项目入门/03-当前状态.md)
5. [后续路线图](docs/01-项目入门/04-后续路线图.md)

按任务继续阅读[配置与能力规范](docs/02-架构与规范/03-配置与能力规范.md)、[界面与交互规范](docs/02-架构与规范/05-界面与交互规范.md)、[测试与验收规范](docs/02-架构与规范/06-测试与验收规范.md)和[业务模块](docs/03-业务模块/README.md)。历史材料只说明发生时的结果，不替代当前规则或代码检查。

## 分支与实施

- 按用户 2026-09-23 的最新要求，日常修改、验证和提交优先在 `development` 进行；保留 `main`，不自动创建功能分支。隔离实验或功能分支仅在用户另有要求时使用。
- 先在本地完成预览、审查和完整验证，再按用户授权推送 `development`。推送不等于发布；合入 `main` 需用户明确安排且发布检查通过。
- 删除功能分支前确认其全部提交已进入 `development`，先确认远端更新成功再清理；不丢弃未合并提交，不强推覆盖未知更新。
- 先明确模块边界、状态归属、类型契约和失败路径。底层设计不支持需求时重构底层与调用方，不向 `demo/App.vue` 堆通用状态、页面特例或重复逻辑。
- 禁止运行旧版 JS、iframe 包装旧页、假按钮、任意动态脚本、类型逃避及临时补丁。旧版 `reference/` 文件保持原样。
- 设置未声明或关闭时隐藏，显式 `disabled` 才只读；显示文字与实际查询值分开，数字和字符串类型不可混同。

## 验证和文档

功能、规则、配置或接口改变时，更新当前状态和相关说明；在[版本记录](docs/04-版本记录/README.md)新增独立说明，并维护月份、分类索引和关联验收记录。遵循[文档归档规则](docs/02-架构与规范/04-文档编写与归档规则.md)。

交付前执行 `npm run verify:release`，其中包含文档审计、可移植性、SFC、类型、单元测试、构建、发布包和浏览器检查。报告实际执行结果与未覆盖边界；不能只凭测试数量声称视觉一致或真实后台已验收。

用户最新明确指令优先于历史计划和旧分支流程。完成后报告修改文件、架构或流程调整、测试结果、提交信息及剩余风险。
